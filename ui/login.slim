javascript:
  $(document).ready(function(){
    $(".signup").keyup(function(){Login();});
  });
h4=i18n.user.form.login
form.vertical name="register" action="/login" method="post"
  lable=i18n.user.form.username 
  input.signup#L_user name="name" type="text" value=(params[:name]) placeholder=(i18n.user.form.username_l) 
  lable=i18n.user.form.password
  input.signup#L_pass name="password"  type="password"  placeholder=(i18n.user.form.password_l)
  button.red#L_input type="submit" disabled="" 
    =i18n.user.form.btn
